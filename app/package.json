{
  "name": "final-project-app",
  "version": "1.0.0",
  "description": "Tier 2 Final Project App",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "lint": "echo 'No linting configured' && exit 0",
    "build": "echo 'Build completed' && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.3",
    "multer": "^2.1.1",
    "dotenv": "^16.0.3"
  }
}
